# MiniLLM v2 — dépendances
torch>=2.3            # déjà installé sur Colab / Kaggle
numpy>=1.24
tokenizers>=0.15      # tokenizer BPE (Rust, très rapide)
datasets>=2.18        # Wikipédia FR / FineWeb-2 / French-Alpaca en streaming (pas nécessaire en 100 % hors-ligne)
matplotlib            # courbes dans le notebook (optionnel)
pytest                # tests (optionnel)
